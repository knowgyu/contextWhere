# contextWhere fallback cron example if systemd user timers are unavailable.
# Prefer: contextwhere autostart install

*/15 * * * * cd /opt/contextWhere && contextwhere daily --json >> logs/contextwhere-daily.log 2>&1
