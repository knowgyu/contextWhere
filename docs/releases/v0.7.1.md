# v0.7.1 — documentation repair

## 요약

0.7.1은 0.7.0 이후 눌린 README/INSTALL command block과 roadmap 문단을 복구한 문서 패치 릴리즈다.

## 변경 사항

- `README.md`를 현재 0.7.x 기능 기준으로 다시 정리했다.
- `docs/INSTALL.md`의 설치/first-run/live-provider 명령을 줄 단위로 복구했다.
- 버전을 0.7.1로 맞췄다.

## 검증

- `python -m compileall -q src tests`
- `pytest -q` → 42 passed
- `python -m contextwhere verify --json` → ok true
