# contextWhere v0.15.2

Keeps durable product contracts while removing generated planning duplication.

## Changed

- Condensed the unique writer-boundary, unavailable-provider, end-to-end
  fixture, scope, and review contracts into
  `context/decisions/0003-preserve-planning-contracts.md`.
- Removed tracked generated `.omx` plans, reviews, interviews, and runtime
  artifacts; future `.omx/` state stays local and ignored.
- Corrected current product documentation to record `where-skills` as already
  retired and archived.

## Verification

- `70` Python tests passed.
- `contextwhere verify --json` passed all smoke steps.
- `python -m compileall -q src tests`
- `git diff --check`

## Still external

Real packaged MailWhere and OfficeWhere calls remain a managed Windows PC check.
