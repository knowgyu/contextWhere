# contextWhere v0.14.0

Makes autostart schedule the safest local maintenance command.

## Changed
- `contextwhere autostart plan/install` now schedules `contextwhere maintain --json` instead of the provider-oriented `run` flow.
- systemd service/timer and Windows task labels use maintain naming.

## Still deferred
- Running scheduler installation in tests.
- Background daemon.
- Live provider scheduling.
